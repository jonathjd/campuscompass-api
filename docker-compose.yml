version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: Mynetflix@2018
    ports:
      - "5432:5432"

  app:
    build: .
    depends_on:
      - db
